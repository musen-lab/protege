# Agent instructions

See `CLAUDE.md` in this directory. It is the single copy of these instructions: where the project's knowledge lives (the team wiki, the generated codebase docs, the guided course), what to file back at the end of a session, and which branching workflow applies.

Nothing agent-specific belongs here. If it ever does, it goes below this line, and everything shared stays in `CLAUDE.md`.
